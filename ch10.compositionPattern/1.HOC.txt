------------------------------------------------------------------------------------

고차함수
- 함수를 인자로 받아 함수를 리턴하는 함수
- 1.인자로 받거나 + 2.함수를 리턴하는 두가지 특성을 모두 가진 함수

------------------------------------------------------------------------------------

고차 컴포넌트
- 컴포넌트를 인자로 받아 개선된 다른 컴포넌트를 리턴하는 함수
- 

const checkAuthorization = () => {
  // 권한 확인
}

const withAuthorization = (Component: React.FC): React.FC => {
  return (props: any) => {
    const isAuthorized = checkAuthorization()

    return isAuthorized ?  <Component {...props} /> : <Login />
  }
}

- 컴포넌트가 그려질떄 권한 체크를 처리

-----------------------------------------------------------------------------------

import React from "react";
import { PanelProps } from "./types";

export const ExpandablePanel = ({
  isOpen,
  toggle,
  heading,
  content,
}: PanelProps) => {
  return (
    <article>
      <header onClick={toggle}>{heading}</header>
      {isOpen && <section>{content}</section>}
    </article>
  );
};

withAutoClose HOC로 열리고 몇초후에 닫히는걸
withKeyboardToggle HOC로 키보드 입력으로 접거나 펼치기를 적용

-----------------------------------------------------------------------------------

HOC는 객체지향의 데코레이터 패턴과 유사
구조를 변경하는대신 추가로 감싸서 동작을 확장

-----------------------------------------------------------------------------------